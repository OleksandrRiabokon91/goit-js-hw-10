name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main] # Запуск при пуше в ветку main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2 # Проверяем код из репозитория

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v3
        with:
          node-version: 18 # Указываем стабильную версию Node.js

      - name: Install dependencies 🔧
        run: |
          npm ci  # Устанавливаем зависимости

      - name: Build project 🔨
        run: |
          npm run build  # Собираем проект

      - name: Deploy 🚀
        run: |
          npm run deploy  # Запускаем деплой через gh-pages
